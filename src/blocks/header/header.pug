mixin b-menu(items)
	each item in items
		+e('li').nav-item(href='#')= item
mixin b-header(menuItems)
	+b('header').header
		+e('img').logo(src=asset('i/logo-main.png'))
		+e.nav
			+b-menu(menuItems)